import { NoWeekendDirective } from './no-weekend.directive';

describe('NoWeekendDirective', () => {
  it('should create an instance', () => {
    const directive = new NoWeekendDirective();
    expect(directive).toBeTruthy();
  });
});
